# worknav — 구현 계획

설계서: [DESIGN.md](./DESIGN.md)

## 원칙

**v0는 리포를 만들지 않는다.** 지금 이 스택 모델이 실제 작업 패턴에 맞는지 모른다. 리포부터 파고 버전을 붙이면 틀렸을 때 버리기가 어려워진다. 2주 쓰고 §미해결 질문(DESIGN §7)에 답이 나온 뒤에 리포로 승격한다.

각 마일스톤은 **그 자체로 쓸모가 있어야 한다.** 다음 단계가 없어도 손해가 아니어야 한다.

---

## v0 — 수동 스택 (목표: 반나절 구현, 2주 사용)

리포 없음. `~/.claude/` 에 직접 둔다.

```
~/.claude/
  scripts/worknav/
    wn.py            # CLI 단일 진입점 (root/push/pop/park/where/tree/inbox/resume)
    store.py         # state.json 읽기·쓰기, 파일 잠금, 원자적 교체
    render.py        # 경로 한 줄 / 트리 / 보류함 렌더
    statusline.py    # 읽기 전용 상태줄
  commands/
    root.md push.md pop.md park.md where.md tree.md inbox.md resume.md
  settings.json      # statusLine 등록 (기존 hooks 보존, 병합)
```

이 단계에 **훅은 없다.** 전부 사람이 슬래시 커맨드로 친다.

이유: 자동 감지를 먼저 만들면 감지가 정확한지, 애초에 스택 모델이 맞는지 모르는 채로 훅을 디버깅하게 된다. 수동으로 2주 쓰면 `depth_warn`, `stale_open_min`, PARK 기본값이 맞는지가 데이터로 나온다.

단, **상태줄은 v0에 넣는다.** 상시 노출이 이 도구 가치의 절반이고, 읽기 전용이라 위험이 없다.

### 구현 순서

1. `store.py` — 스키마 + 파일 잠금 + 원자적 교체. 여기가 틀리면 나머지가 전부 무의미하다.
2. `wn.py root/push/pop/park` — 4개 연산.
3. `render.py` + `statusline.py` — 표시.
4. `wn.py where/tree/inbox/resume` — 조회.
5. `commands/*.md` — 슬래시 커맨드 8개.
6. `settings.json` 에 `statusLine` **병합** (기존 `hooks` 블록 훼손 금지 — 현재 `PreToolUse:Agent`, `UserPromptSubmit` 두 개가 살아 있다).

### v0 인수 조건

전부 자동 테스트로 확인 가능해야 한다.

- **T1 왕복** — `root → push A → push B → pop → pop` 후 커서가 루트, A·B는 `done`, POP마다 부모의 `resume_note` 가 정확히 출력됨
- **T2 깊이 게이트** — 깊이 3 `push` 는 `--force` 없이 exit 3, 상태 파일 변경 없음
- **T3 복귀지점 강제** — `resume_note` 없이 `push` 시 exit 2, 상태 변경 없음
- **T4 PARK 커서 불변** — `park` 후 커서 id가 이전과 동일
- **T5 TTL** — 8일 지난 `parked` 노드가 `/inbox` 에서 만료 표시됨. 자동 삭제 안 됨
- **T6 상태줄 무결성** — `state.json` 없음 / 빈 파일 / 깨진 JSON 세 경우 모두 빈 출력 + exit 0
- **T7 동시성** — 두 프로세스가 동시에 `park` 100회씩 → 노드 200개, id 중복 없음, 유실 없음
- **T8 깊이 파생** — `parent` 체인이 5단계일 때 `where` 의 깊이가 5. 어디에도 `depth` 필드가 저장되지 않음

### v0 사용 기간에 수집할 것

`wn.py` 가 `.claude/worknav/events.jsonl` 에 append-only로 남긴다.

- 게이트 발동 횟수 / 그중 `--force` 통과 비율 → `depth_warn` 조정 근거
- park된 항목 중 실제로 `resume` 된 비율 → 보류함이 쓰레기통인지 판정
- POP 시점의 노드 체류 시간 분포 → `stale_open_min` 조정 근거
- 최대 도달 깊이 분포

---

## v0.5 — 훅 자동화 — **완료**

계획서에는 "v0 을 2주 쓴 뒤" 로 적어 뒀지만, 수동 전용 v0 은 명령을 쳐야 한다는 것
자체가 "지금 내가 가지를 치고 있다" 는 자각을 전제로 하고, 그 자각이 없는 게 원래
문제였다. 그래서 순서를 앞당겨 훅까지 붙였다. 대가는 명확하다 — 이제 검증할 것이
스택 모델 하나가 아니라 스택 모델과 감지 정확도 둘이고, 안 맞을 때 어느 쪽 탓인지
구분이 어렵다. `auto: true` 플래그와 `events.jsonl` 의 `result: "auto"` 가 그 구분을
사후에 가능하게 만드는 장치다.

만든 것:

- `hooks/hooks.json` + 훅 **5종** (계획은 4종이었다. `PostToolUseFailure` 가 추가됐다 — 아래 참조)
- `src/detect.py` — 정규식 가지 감지 (LLM 아님)
- `src/hookrt.py` — stdin/stdout 계약, 출력 예산, fail-safe 러너
- `src/hooklogic.py` — 훅 4종의 로직. 순수 함수라 훅을 띄우지 않고 단위 테스트된다
- `hooks/*.py` — 얇은 진입점 4개

### 검증 루프가 실제로 잡은 것

단위 테스트만으로는 하나도 못 잡았을 것들이다. 전부 실물을 돌려서 나왔다.

1. **`PostToolUseFailure` 누락 (치명적).** 설계 초안에 이 이벤트가 없었다. 성공한 툴만
   `PostToolUse` 로 오고 **실패한 툴은 별도 이벤트로 가며, 내용도 `tool_response` 가
   아니라 최상위 `error` 문자열에 담긴다.** 가지의 주요 원천이 실패한 명령인데 그게
   통째로 빠져 있었다. 격리 환경에서 실제 세션을 돌려 "에러를 냈는데 아무것도 안
   담긴다" 를 보고서야 발견했다.
2. **`ValueError` 를 못 잡는 정규식.** `\bError\b` 는 `ValueError` 에서 단어 경계가
   성립하지 않아 매칭되지 않는다. 파이썬 예외는 거의 전부 이 형태다. 스모크 실행
   첫 줄에서 드러났다.
3. **`str` 에 `.get()` 호출.** `PostToolUseFailure` 를 붙이면서 생긴 버그인데,
   **fail-safe 가 예외를 삼켜서 밖에서는 "아무것도 안 담긴다" 로만 보였다.**
   원인을 짚을 단서가 전혀 없었다. 그래서 `WORKNAV_HOOK_DEBUG=1` 진단 스위치를 넣었다.
4. **환경변수 이름이 틀렸다.** v0 이 `CLAUDE_SESSION_ID` 를 읽고 있었는데 실제 이름은
   `CLAUDE_CODE_SESSION_ID` 다. 노드의 `session_id` 가 계속 null 이었다.
5. **트레이스백 제목이 무의미했다.** 첫 줄을 담으면 보류함에
   "Traceback (most recent call last)" 만 남아 어떤 사건인지 알 수 없다. 마지막 줄로 바꿨다.

### v0.5 인수 조건 — 결과

- **T9 fail-safe** — 통과. 상태 없음·깨진 JSON·쓰레기 stdin·강제 예외·잠긴 상태 다섯
  경우에 대해 전 훅이 `exit 0`, stdout·stderr 모두 빈 값. 진단 스위치를 켜면 터진다.
- **T10 예산** — 통과. 컨텍스트 주입 ≤ 400자(줄 경계에서 자름), 알림 = 1줄 ≤ 80자.
- **T11 무의존** — 통과. `python3 -S` 로 훅 4종 실행, 그리고 `src/`·`hooks/` 전 파일의
  import 문을 표준 라이브러리 화이트리스트와 대조.
- **T12 지연** — 통과. `on_tool.py` 20회 실행 p95 < 200ms.
- **T13 오탐률** — **아직 못 잼.** 실제 사용 데이터가 필요하다. 재는 방법은 준비돼 있다 —
  `events.jsonl` 에서 `result: "auto"` 를 세고, 그중 `dropped` 로 끝난 비율을 보면 된다.
  40% 를 넘으면 감지 임계를 올린다.

추가로 통과한 것: 자동 적재가 커서를 움직이지 않음, 어떤 감지로도 depth 가 늘지 않음,
중복 억제, 상한 도달 시 정지, 루트 없으면 상태 파일조차 만들지 않음, `Stop` 이
`decision: block` 을 내지 않음, 실측 페이로드 두 종(성공·실패) 그대로 통과.

### 종단 검증 (실제 Claude Code 세션)

격리된 `CLAUDE_CONFIG_DIR` 에 플러그인으로 설치하고 진짜 세션을 돌렸다.

- 훅 5종 전부 인식 (`plugin details` 기준, harness-only 라 모델 컨텍스트 비용 0)
- 실패한 명령 → 보류함에 `ValueError: seed must be an integer, got str` 자동 적재
- 같은 명령을 두 번 실패시켜도 하나만 담김 (중복 억제)
- `Write` 로 쓴 `# TODO: 로깅 포맷 통일 필요` → 자동 적재
- **깊이 3에서 모델이 멈추고 사용자에게 물었다.** 스스로 `--force` 를 붙이지 않았고
  상태는 `d2` 그대로였다. 이 도구의 핵심 동작이 실제로 작동한다.

## v1 — 배포 (전제: v0.5를 2주 쓰고 T13이 통과했을 때)

- `.claude-plugin/marketplace.json` 추가 → 남이 설치 가능
- README, 설정 옵션 문서화 (`depth_warn` 등을 사용자가 조정 가능하게)
- OpenClaw 2차 진입점 — Slack에서 "이거 보류함" 한 줄이 같은 `state.json` 에 들어가게. 스키마 소유권은 플러그인, OpenClaw는 얇은 write 클라이언트

v1에서 처음으로 "남이 쓸 때 어떤가"를 고민한다. 그 전까지는 1인 도구로 만든다.

---

## v0-deploy — GitHub 배포 (완료)

`KeyLucky/claude-worknav` (public). 리포 자체가 마켓플레이스다.

```
/plugin marketplace add KeyLucky/claude-worknav
/plugin install worknav@worknav
```

격리 환경(`CLAUDE_CONFIG_DIR`)에서 확인한 것.

- GitHub 소스로 `marketplace add` → `plugin install` → 커맨드 8개 인식
- 설치된 캐시 경로에서 `root` → `push` → `park` → `path` → `tree` 실제 실행
- **SSH 키가 없어도 된다.** CLI 가 먼저 `git@github.com:` 으로 시도하고 실패하면
  `https://` 로 자동 폴백한다. `GIT_SSH_COMMAND=/bin/false` 로 막아 확인했다 —
  Windows 사용자 대부분이 이 경로를 탄다
- clone 후 `npm test` 26개 통과, `vsce package` 로 `.vsix` 생성까지

파일은 GitHub API 로 올렸다. **한글이 깨졌다.** clone 후 로컬과 `diff` 로 전수 대조해서
잡았다. **전송 후 대조는 생략하면 안 된다.**

당시 "한글 세 글자" 로 적었는데 **둘 다 틀렸다.** v0.5 배포 때 다시 전수 대조해 보니
그때 못 잡은 손상이 vscode-ext 에 그대로 남아 있었고(`뜹니다`→`뚜니다`, `담아뒀다`→`담아둑다`,
`옮깁니다`→`옛깁니다`, `통째로`→`통 `, `스텁`→`스텀`, `끊는다`→`끕는다`, `끝`→`끕`,
`흩어`→`흔트려`, `바뀌면`→`바뀜면`, `덮어씀`→`덮어쐬`), 개수도 훨씬 많았다.

**원인도 전송이 아니었다.** API 가 바이트를 망가뜨린 게 아니라, 업로드할 때 파일 내용을
`\uXXXX` 이스케이프로 손수 옮겨 적으면서 코드포인트를 잘못 쓴 것이다. 한글은 자모가
한 코드포인트에 합쳐져 있어서 한 자리만 틀려도 다른 글자가 되고, 눈으로는 잘 안 띈다.

교훈 둘. **파일 내용을 손으로 재입력하지 않는다** — 옮길 때는 한글을 이스케이프하지 말고
원문 그대로 넘긴다(v0.5 코드 9개는 그렇게 올려 전부 바이트 단위로 일치했다). 그리고
**대조는 바뀐 파일이 아니라 리포 전체에 돌린다** — "고쳤다" 고 적어 둔 것이 실제로 고쳐졌는지는
전수 대조만이 안다.

`.vsix` 는 리포에 두지 않는다. 바이너리라 API 로 못 올리고, 무엇보다 소스에서 언제든
다시 만들 수 있다. README 에 빌드 두 줄을 적어 뒀다.

---

## v0-portable — Windows 포함 이식성 (완료)

다른 환경에 배포하려면 POSIX 가정을 걷어내야 했다. `install.sh` 로 자기 머신에만 깔 때는
드러나지 않던 것들이다. 셋 다 **Windows 에서 조용히 실패하는** 종류였다.

| 지점 | 증상 | 대응 |
|---|---|---|
| `import fcntl` | Windows 에 없음 → 모듈 import 자체가 실패 | `fcntl` / `msvcrt` 를 선택적으로 import 하고 `_try_lock` / `_unlock` 으로 감쌈 |
| `select.select(sys.stdin)` | Windows 의 select 는 소켓만 받음 → 예외 | 데몬 스레드 + `join(timeout)` 으로 교체. 플랫폼 분기가 사라짐 |
| 콘솔 인코딩 | cp949 에서 `⟩ › ⑂` 가 `UnicodeEncodeError`, statusline 의 except 가 삼켜서 **빈 줄** | `force_utf8_output()` 으로 stdout/stderr 재구성 |

세 번째가 가장 나빴다. 예외가 밖으로 안 나오고 표지판만 사라지므로, 사용자 입장에서는
"설치했는데 아무것도 안 뜬다"가 된다. 원인을 짚을 단서가 없다.

`os.replace` 도 손봤다. POSIX 는 읽는 중인 파일을 교체해도 성공하지만 Windows 는
`PermissionError` 를 낸다. 표시 쪽이 파일을 여는 시간은 밀리초라 짧게 재시도한다.

인수 조건 W1~W7 (`tests/test_portability.py`). POSIX 전용 모듈을 강제로 없는 것처럼 만들어
같은 코드 경로를 통과시킨다.

- W1 잠금 수단이 아예 없어도 상태는 저장된다 (길잡이가 작업을 막으면 안 된다)
- W2 `fcntl` 이 없으면 msvcrt 경로로 넘어간다 — 잠금/해제 구간이 같은지까지 확인
- W3 잠금이 잡혀 있으면 어느 플랫폼이든 매달리지 않고 exit 5
- W4 `replace` 가 두 번 실패해도 재시도로 성공한다
- W5 `PYTHONIOENCODING=cp949` 하위 프로세스에서 표지판이 정상 출력된다
- W6 `force_utf8_output` 은 어떤 스트림에서도 예외를 내지 않는다
- W7 stdin 이 비어 있어도 상태줄이 매달리지 않는다

W5 는 수정을 되돌려 실패하는 것까지 확인했다 (되돌리면 stdout 이 빈 문자열).

**여기서 증명되지 않는 것** — msvcrt 잠금이 실제 Windows 에서 배타성을 갖는지. 그건 Windows 에서
T7(두 프로세스 동시 park)을 돌려야만 안다. 세션 하나로 쓰는 한 문제되지 않는다.

---

## v0-plugin — 플러그인 구조 전환 (완료)

v1 로 미뤄뒀던 것을 앞당겼다. 이유는 하나 — Claude Code VS Code 확장 환경에서는 터미널의
`claude` 명령을 못 쓰는 경우가 있고, 확장 UI 의 Manage Plugins → Marketplaces 가 사실상
유일한 설치 경로다. `install.sh` 는 그 경로로 쓸 수 없다.

바이너리에서 확인한 마켓플레이스 소스 종류: `github` / `url` / `npm` / `git-subdir` /
`file` / `directory`. **`directory` 가 로컬 폴더를 그대로 받는다** — 리포를 GitHub 에
올리지 않아도 절대 경로만으로 설치된다.

한 작업만 필요했다.

- 커맨드 8개의 `python3 ~/.claude/scripts/worknav/wn.py` → `python3 "${CLAUDE_PLUGIN_ROOT}/src/wn.py"`
- `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` 추가 (`source: "./"`)
- `install.sh` 는 복사 시 그 변수를 실제 경로로 치환하도록 수정 — 두 설치 방법이 공존한다

격리 검증 (`CLAUDE_CONFIG_DIR=/tmp/...` 로 실제 `~/.claude` 를 건드리지 않고).

- `claude plugin validate` — 매니페스트 2개 통과
- `marketplace add <로컬 절대경로>` → `plugin install worknav@worknav` 성공
- `plugin details worknav` — 커맨드 8개 전부 인식, always-on 약 94토큰
- 설치된 캐시 경로(`plugins/cache/worknav/worknav/0.1.0/src/wn.py`)에서 `root` → `push` →
  `park` → `path` 를 실제로 돌려 표지판 문자열까지 확인

부수 효과 하나 — 플러그인 설치 시 리포 전체가 복사되므로 `vscode-ext/*.vsix` 도 캐시에 함께 들어간다.

---

## v0-vscode — VS Code 상태 표시줄 (완료, `vscode-ext/`)

v0.5보다 먼저 만들었다. 순서를 앞당긴 이유는 하나다 — 주 작업 환경이 Claude Code VS Code 확장의
사이드바 채팅 UI인데, 거기서는 터미널 상태줄이 원리적으로 보이지 않는다 (DESIGN §5.4). 표지판이
안 보이면 v0를 2주 써볼 수도 없다.

구성물은 셋. 외부 의존성 없음.

- `src/render.js` — `render.py` 의 이식본 (`path_line` / `tree` / `inbox` / `tooltip`)
- `src/locate.js` — `store.project_root` 와 같은 git 루트 탐색
- `extension.js` — 상태 표시줄 항목, 파일 변경 감시, 명령 3개

인수 조건 26개 통과 (`npm test`). 나눠 보면 이렇다.

- **X1–X2 크로스 체크** — 같은 상태에서 파이썬 렌더러와 JS 렌더러의 출력이 문자 단위로 일치.
  이게 이 확장에서 가장 중요한 테스트다. 표지판 두 개가 다른 말을 하면 둘 다 못 믿게 된다
- **E1–E9 종단** — 실제 `wn.py` 를 돌려 만든 상태 파일로 확장을 구동한다. 합성 픽스처만 쓰면
  CLI 가 실제로 쓰는 스키마와 어긋나도 통과하므로, 여기서는 CLI 를 그대로 부른다
- **R1–R12 / L1–L3** — 깨진 입력, 폭 압축 종료, parent 순환, git 루트 경계

육안 확인에서 잡은 것 (테스트만으로는 안 나왔다).

- `package.json` 의 `test` 스크립트가 `node --test test/` 라 실행 자체가 실패했다
- `alignment` 설정을 바꿔도 반영되지 않았다 (항목 생성 시점에만 읽음) → 재생성하도록 수정
- 노드 제목에 `$(` 가 들어가면 VS Code 가 아이콘 문법으로 해석해 표지판 한가운데가 사라진다
  → 상태 표시줄로 나갈 때만 끊는 후처리 추가
- 좁은 폭에서 압축이 예산을 못 지키는 하한이 존재한다. 이건 버그가 아니라 "루트와 현재는
  버리지 않는다"의 대가다. 테스트로 고정하고 README 에 명시했다

`npx @vscode/vsce package` 로 `.vsix` 생성까지 확인했다 (12KB, 테스트 파일 제외).

---

## v0.5-hardening — 견고성 라운드 (완료)

코드를 훑어 "오류가 나기 쉬운 곳" 을 찾고, 유형별로 실제 값을 넣어 찔러 보고,
깨진 곳을 고치고 테스트로 고정했다. **12곳**이 나왔다.

### 왜 찔러 봐야 했나

전부 눈으로 코드를 읽어서는 안 나오고 값을 넣어야 나오는 것들이었다. 그리고 절반은
**조용히 실패하는** 종류였다 — 훅의 fail-safe 가 예외를 삼켜서 밖에서는
"아무것도 안 뜬다" 로만 보인다. v0.5 에서 이미 한 번 그렇게 놓쳤는데, 같은 모양의
구멍이 `detect` 타입 가드, `hookstate.json`, `events.jsonl` 세 곳에 더 있었다.

### 고친 것

| # | 지점 | 증상 |
|---|---|---|
| 1 | `store.load` 스키마 검증 없음 | 손상된 파일에서 **파이썬 스택 노출** (4가지 모양) |
| 2 | 부모가 사라진 노드 | 손상인데 **사용자 오류(exit 2)로 둔갑**. `tree` 는 조용히 노드를 빠뜨림 |
| 3 | 순환 탐지에 깊이 상한(1000) 사용 | **정상적으로 깊은 트리를 순환이라고 오진** |
| 4 | `render.tree` 재귀 | 깊이 ~990 에서 `RecursionError` |
| 5 | `path_line` 압축이 O(깊이²) | **깊이 3000 에서 4.1초** — 매 렌더마다 도는 경로 |
| 6 | 제목의 ANSI·제어문자 | 표지판이 커서를 움직여 화면을 망가뜨림 |
| 7 | 파이썬 / JS 렌더러 불일치 | 손상 입력에서 한쪽은 예외, 한쪽은 빈 줄 |
| 8 | `wn.py` 가 `OSError` 미처리 | 읽기 전용 디렉터리 등에서 스택 노출 |
| 9 | JS `title` 숫자 / `nodes` null | 확장에서 `TypeError` |
| 10 | `detect` 타입 가정 | 페이로드 모양이 다르면 **조용히 감지 실패** |
| 11 | `config` 값 타입 가정 | `title_max: "많이"` 한 줄에 표지판이 죽음 |
| 12 | `hookstate.json` / `events.jsonl` 타입 가정 | 객체가 아니면 훅이 **조용히 죽음** |

성능은 4131ms → 3ms (깊이 3000 상태줄). 지름길이 원래 압축 결과와 같은지는 수식을
전수 비교해 확인했고, 그 비교 자체를 테스트로 남겼다.

### 인수 조건 R1~R10

- **R1** 손상된 상태 17가지 x 표시 경로 5곳 — 전부 exit 0, stdout·stderr 빈 값
- **R2** 같은 17가지 x CLI 8개 — 전부 exit 4, 스택 노출 없음, 쓰기 명령은 파일 무변경
- **R3** 깊이 1500 — 순환 오진 없음, 스택 안 터짐, 진짜 순환은 여전히 잡음
- **R4** `detect` 에 이상한 타입 20가지 — 예외 없음, 정상 감지는 유지
- **R5** 제어문자 6종 — 표지판·트리·보류함 어디에도 안 나감, 저장 단계에서 걸림
- **R6** 파일시스템 실패 4가지 — 스택 노출 없음, 훅은 조용
- **R7** 두 렌더러가 손상·깊은 입력에서 문자 단위로 일치
- **R8** 깊이 3000·노드 5000 에서 상태줄 < 100ms + 지름길 동치성 전수 비교
- **R9** 잘못된 `config` 10가지 — 기본값으로 복귀
- **R10** `hookstate`·`events` 이상 값 — 훅 로직과 실제 스크립트 양쪽에서 조용

전체 **145개 통과** (본체 119 + 확장 26).

### 확인했고 문제가 없던 것

다음에 같은 곳을 다시 의심하지 않기 위해 적어 둔다.

- `events.jsonl` 동시 append — 세 프로세스 x 60회, 한 줄 9KB 에서도 유실 0
- 동시 `park` — 세 프로세스 x 40회, 유실 0 · id 중복 0 · 사후 검증 통과
- `detect` 정규식 — 20만 자 한 줄, `"Error "` 2만 반복에서 4ms (백트래킹 폭발 없음)
- `edit()` 중 예외 — 잠금이 풀리고 다음 명령 정상
- 실제 파일 깊이 400(139KB) — 상태줄 23ms, 훅 28~48ms

---

## WIP 상한 (너비 게이트) — K1~K14

깊이 게이트는 *아래로 깊어지는 것*만 막았다. 원래 문제 진술("가지가 기하급수적으로
늘어남")은 옆으로 퍼지는 쪽에 더 가까운데, 형제가 15개 열려 있어도 경고가 없었다.

`wip_limit = 3` 은 이 프로젝트에서 유일하게 **사용 데이터를 기다리지 않고 정한 임계값**이다.
칸반의 1인당 WIP 상한이 이미 검증된 값이라 내가 지어낼 필요가 없었다.

### 인수 조건

- **K1** 상한에서 `push` 는 exit 3, 상태 JSON 전체가 바이트 동일
- **K2** `--force` 통과, `wip_forced` 로 따로 기록 (깊이 `forced` 와 섞이지 않음)
- **K3** 루트는 세지 않는다 (`wip_limit=1` 에서 첫 `push` 가 통과)
- **K4** 보류함은 세지 않는다 — 12개 담아도 `push` 정상, `park` 자체는 절대 안 막힘
- **K5** `resume` 도 게이트에 걸린다 (없으면 보류함으로 상한이 통째로 우회됨)
- **K6** `config.wip_limit` 로 조정
- **K7** `wip_limit: "많이"` / `0` → 기본값으로 복귀, 예외 없음
- **K8** 둘 다 걸리면 깊이 문구. 이벤트도 `gate` 하나만 (한 시도를 두 번 세지 않음)
- **K9** 거절당한 시도는 `stats` 의 판정 횟수에서 제외, WIP 게이트로 따로 집계.
  표본 5개 미만이면 조정 제안을 아예 안 한다
- **K10** 게이트 문구가 열린 노드를 전부 나열하고 루트는 빼며, 어떤 제목에서도 한 줄이 100칸 이하
- **K11** 게이트의 시간은 **열려 있던 시간**이다 (`reopened_at`). 3일 전 담긴 보류를
  방금 꺼냈는데 "4320분째" 라고 쓰면 무엇을 닫을지 판단을 망친다
- **K12** `done`/`dropped` 는 안 센다 — `pop` 하면 자리가 난다
- **K13** 상한 경계에서 세 프로세스가 동시에 밀어도 상한을 넘지 않는다 (검사가 잠금 안에 있다)
- **K14** 상한에 닿았다는 사실이 매 턴 모델에게 주입되고, 최악의 예산에서도 규칙과 함께 살아남는다

전체 **198개 통과** (본체 172 + 확장 26).

### 검증 루프에서 잡힌 것

- **`resume` 이 상한을 통째로 우회했다.** 처음엔 `push` 에만 걸었다. 그런데 폭이 실제로
  늘어나는 경로가 `resume` 이다 — 순수한 `push` 반복은 깊이가 먼저 걸려서 폭이 안 늘어난다.
  테스트 시나리오를 짜다가 "이 게이트에 도달하는 경로 자체가 `resume` 밖에 없다"는 걸
  알고 발견했다.
- **게이트 문구가 화면을 넘어갔다.** `render.truncate` 는 글자 수로 자르는데 한글은 한 자가
  두 칸이다. 깊이 게이트는 아예 자르지도 않고 있었다(기존 결함). 칸 수로 자르는 `_clip` 을
  따로 뒀다 — `render.truncate` 는 JS 렌더러와 문자 단위로 일치해야 해서 못 건드린다.
- **게이트의 시간이 틀렸다.** `opened_at` 은 "만들어진 시각"이라 보류함에서 꺼낸 노드는
  담긴 시각이 나온다. 육안 확인에서만 보였다 — 테스트는 전부 방금 만든 노드라 0분이었다.
- **모델이 상한을 모른 채 시도했다.** 게이트가 강제하는 것과 모델이 아는 것은 별개다.
  훅 출력을 직접 읽어 보고서야 알았다. 매 턴 왕복이 하나 낭비되고 있었다.
- **예산 검사의 빨간불을 확인했다.** `CONTEXT_BUDGET` 을 330 으로 낮추니 WIP 줄이 잘려
  나가며 실패한다. 최악 실측은 411자 / 예산 520.

---

## 세션 복원과 자가진단 — D1~D7, DR1~DR9

`planning-with-files` 에서 배관 두 개를 가져오려다, 확인해 보니 하나는 애초에
불가능한 방법이었고 대신 더 싼 답이 나왔다.

### 압축 대응 — PreCompact 가 아니라 SessionStart (D1~D7)

`PreCompact`/`PostCompact` 는 입력 이벤트로만 존재하고, 훅 **출력** 스키마의
`hookEventName` 목록에 없다(2.1.251 바이너리 실측). 즉 `additionalContext` 를
돌려줄 수 없어서 주입 통로가 아니다.

대신 `SessionStart.source` 가 다섯 값을 받는다 — `startup·resume·clear·compact·fork`.
매처가 `startup|resume` 이라 셋을 통째로 버리고 있었다. 매처 한 줄로 해결된다.

- **D1** 다섯 source 가 매처에 있고, Compact 계열은 주입 통로가 아님을 코드 옆에 고정
- **D2~D4** compact / clear / fork 각각 상황 안내 + 경로 + 분기규칙. 셋이 서로 다른 문구
- **D5** startup/resume 은 기존 그대로. 모르는 source 도 죽지 않고 평소 배너
- **D6** 상태 없음·손상이면 다섯 source 전부 침묵 (옵트인 유지)
- **D7** 최악의 예산에서도 분기규칙 마지막 줄까지 생존. 예산을 줄이면 실제로 잘림(빨간불)

곁들여 잡은 것 — **복귀지점에 상한이 없었다.** 사람이 자유롭게 쓰는 문장이라
그 한 줄이 예산을 다 먹고 분기규칙을 통째로 밀어낼 수 있었다. D7 을 넣고서야 보였다.

### `/wn-doctor` (DR1~DR9)

설계 원칙은 하나다 — **설정을 읽지 말고 실제로 돌려본다.** 이 프로젝트의 실패는
전부 "등록은 돼 있는데 안 돈다" 였고, 매니페스트 검사로는 하나도 못 잡는다.

- **DR1** 상태가 없어도·손상돼도·경로가 파일이어도 rc=0 이고 끝까지 간다.
  한 검사가 터져도 나머지는 돈다
- **DR2** 진단이 사용자 상태를 바꾸지 않는다. 훅을 진짜로 돌리되 임시 프로젝트를
  가리키게 한다 — on_tool 은 보류함에 담는 부작용이 있다
- **DR3** 훅 4종 실제 구동. 깨진 훅(rc≠0)과 모양이 틀린 출력을 잡는다(빨간불 확인)
- **DR4** "훅은 돌았는데 주입 0턴" 을 잡되 오탐은 안 낸다
- **DR5** 손상을 손상이라고 말하고 다음에 칠 명령을 준다
- **DR6** 설치 캐시가 마켓플레이스 사본과 바이트로 다르면 잡는다
- **DR7** 두 표지판이 어긋나거나 `.vsix` 가 소스보다 낡으면 잡는다
- **DR8** 통과는 한 줄, 실패만 조치를 보여준다
- **DR9** 설치본에서 종단 실행. 모든 항목이 JSON 직렬화 가능

전체 **234개 통과** (본체 208 + 확장 26).

### 검증 루프에서 잡힌 것

- **`주입 기록` 이 오탐을 냈다.** 처음엔 `events.jsonl` 의 `session` 필드를 근거로
  썼는데, 세션 안의 셸에서 CLI 를 치기만 해도 환경변수가 붙어 그 필드가 채워진다.
  갓 만든 프로젝트가 곧바로 ✗ 로 떴다. 근거를 **훅만 쓰는 `hookstate.json`** 으로
  바꿨다 — 있으면 훅이 돈 것이다.
- **마켓플레이스 판정의 근거가 약했다.** 처음엔 mtime 비교로 "낡았다" 고 말하려
  했는데, 원격이 앞서 있는지는 네트워크 없이 알 수 없다. 추측해서 말하지 않고
  커밋과 날짜만 보여준다. 대신 네트워크 없이 확실한 것(캐시와 클론의 바이트 대조)을
  검사로 넣었다 — 9/3 의 실제 사고가 이쪽이었다.
- **D2 의 기대값이 틀렸다.** push 는 복귀지점을 **부모**에 적으므로 갓 push 한
  노드에는 없는 게 정상이다. 시나리오를 push→push→pop 으로 고쳤다. D7 도 같은
  이유로 긴 복귀지점이 배너에 안 실리고 있어서, 상한을 시험하지 못하고 있었다.

---

## 하지 않기로 한 것

기록해 둔다. 나중에 "이거 왜 없지" 할 때 다시 논의하지 않기 위해서.

- **자동 PUSH** — 오탐 한 번의 비용이 너무 크다 (DESIGN §4.4)
- **작업 중 전체 트리 표시** — 눈이 형제 노드로 가서 가지치기를 부추긴다 (DESIGN §2)
- **감지의 LLM 분류** — 매 툴 호출마다 부르면 느리고 비싸다. 정규식 오탐률(T13)을
  실측한 뒤에 판단한다 (DESIGN §4.3)
- **주제 이탈 감지** — LLM 이 필요하다. 시간 기준만으로도 대부분 걸린다 (DESIGN §7)
- **`PreCompact` 로 주입** — 그 이벤트는 출력 스키마에 없다. SessionStart 로 간다
- **훅의 `settings.json` 자동 등록** — 남의 설정에 살아 있는 `hooks` 블록을 병합하는 건
  실패 시 손해가 크다. 플러그인 설치가 그 일을 대신한다 (DESIGN §7)
- **세션별 커서** — 실제로 동시 세션을 그렇게 쓰는지 모른다. 문제가 생기면 그때 (DESIGN §2.2)
- **일정 추정 / 티켓 연동** — 비목표 (DESIGN §0)
